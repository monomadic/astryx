@route path="/"
  %html
    %body
      %h1 Index Page

      for post in ./examples/*.md
        let meta = post.frontmatter()

        %a { href: meta.route }
          meta.title
        
        @route path=meta.route
          %h1
            meta.title
          
          post.markdown()
